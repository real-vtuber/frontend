import React from 'react'
import AddNewLiveSession from '../../pages/AddNewLiveSession'

/**
 * Add New Live Session route page
 * Admin interface accessible at /add-new-live-session
 */
export default function AddNewLiveSessionPage() {
  return (
    <AddNewLiveSession />
  )
} 